## 1. Model family parity

- [ ] 1.1 In `providers/anthropic/models_test.go` and `convert_request_test.go`, first table-test direct hyphen-date/undated and resolved Vertex `@`-date/undated Sonnet/Opus 4, specific 4.1/4.5/4.6/4.7/4.8 and 5 families, and unknown Claude/non-Claude fallbacks; assert max tokens, thinking, sampling warnings and unchanged request model IDs, including a failing dated-base regression.
- [ ] 1.2 Correct the generic Sonnet/Opus 4 family matcher in `providers/anthropic/models.go` without changing `ResolveVertexModelID` or reordering specific family precedence; rerun the targeted capability/request tests.

## 2. Web tools and validation

- [ ] 2.1 First add a table-driven request matrix in `providers/anthropic/convert_request_test.go` for all six supported web search/fetch variants: projected camelCase fields, exact native type/name, omitted version-inapplicable fields, header beta selection/deduplication, explicit `useCache: false`, and `responseInclusion` enum on 20260318.
- [ ] 2.2 Add tests for invalid supported web args (non-numeric/null, citations without boolean `enabled`, userLocation without `type: "approximate"` or with invalid optional fields, domain lists and enum), unknown-key stripping and unknown-tool warnings, asserting errors before either generate or stream HTTP request; separately assert fractional and out-of-int64 *finite* numbers are preserved on the wire, even after an unsupported tool was skipped. With `ToolChoiceNone` removing a web tool, test both an empty final tool list and a non-native JSON response-format fallback that subsequently appends a function tool; neither may receive the removed web tool's numeric override.
- [ ] 2.3 Extend `validateProviderToolArgs` in `providers/anthropic/convert_request.go` to validate declared web-variant args before HTTP without changing advisor or unsupported ID handling; add 20260318 SDK union projections and beta selection to `convertProviderTool`. For upstream-valid fractions/out-of-int64 numbers, use `option.WithJSONSet` on the final serialized tool index rather than truncating SDK `int64` fields; guard empty/removed tool lists and cover both request methods.
- [ ] 2.4 Add 20260318 entries in `providers/anthropic/tool_name_mapping.go` and targeted choice/history/generate/stream name round-trip tests in `tool_name_mapping_test.go` and existing response tests.

## 3. Boundary and evidence

- [ ] 3.1 Capture direct and Vertex generate/stream HTTP requests with valid mixed function and server tools; verify strict on supported/unsupported models, inputExamples, Anthropic provider options, empty-tool auto/required/named/none choice, variant-specific betas and aliases without function/server leakage. Retain established malformed function example/option behavior.
- [ ] 3.2 Add a deterministic Go `test/integration/testserver/` stream scenario emitting provider-executed search and fetch parts with distinct custom 20260318 tool names, plus a matching `test/integration/` Vitest test parsing SSE with `parseJsonEventStream`/`uiMessageChunkSchema`, checking chunk tool names and assembled messages with `readUIMessageStream`. Keep native-name aliasing proved separately by Anthropic adapter generate/stream tests; this scenario proves UI consumption of the mapped names without claiming live provider evidence.
- [ ] 3.3 Compare request expectations and Go behavior against pinned commit `08ae5ad05bc12496dd1ffcf64e34419e0831300d` (`@ai-sdk/anthropic` 4.0.58); run `cd providers/anthropic && go test ./...`, `mise run test-integration`, and `mise run parity-check`. Inspect existing provider request fixtures for eligible changes, and do not add or modify recorded provider inputs without a real recording or import without an indexed, byte-identical pinned fixture.
- [ ] 3.4 At review, record any remaining provider-live acceptance gap separately from request/synthetic tests; update `test/conformance/PARITY.md` only if stable evidence/status or support boundaries actually change. Avoid caller-metadata changes and unrelated frontend behavior.
