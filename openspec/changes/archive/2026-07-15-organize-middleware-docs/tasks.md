## 1. Middleware documentation section

- [x] 1.1 Create `docs/middleware/` and move the middleware overview and four integration pages to their descriptive paths.
- [x] 1.2 Update page content and navigation footers for the new directory structure.

## 2. Navigation and contributor guidance

- [x] 2.1 Add the middleware section to `docs/README.md` and update all repository-owned links to moved pages.
- [x] 2.2 Update `CONTRIBUTING.md` with the new taxonomy and middleware documentation location.
- [x] 2.3 Apply the approved taxonomy change to the active `docs-structure` specification.

## 3. Validation

- [x] 3.1 Run documentation lint and verify no active links reference the old middleware guide paths.
- [x] 3.2 Run strict validation for the OpenSpec change and active `docs-structure` specification.
