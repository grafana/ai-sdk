# Validation and rollout evidence

## Local simplified-revision checks

- `openspec validate enable-candidate-source-ci`, `git diff --check`, `mise run test-ci-workflow`, `mise run typecheck-conformance`, `mise run test-module-policy` — pass. Structural workflow tests inspect required job identities, absence of source-to-artifact dependency paths, selected source commands, ordered standalone-before-image steps, push guards and publication/deployment dependencies; they do not simulate GitHub Actions scheduling. The small module-policy fixture selects and executes candidate root/client despite an older declared root pin, then rejects a regressed candidate behavior; separate fixtures reject unmerged/unverifiable ancestry and forbidden reverse dependencies.
- `mise run build`, `mise run vet`, `mise run test-short`, `mise run lint`, `mise run lint-docs` — pass for candidate SDK, providers, middleware, examples and Gateway.
- `mise run test-providerwire-v4`, `mise run test-integration`, `mise run test-ai-gateway-source-integration`, `mise run parity-check` — pass, including candidate Go-client differential, pinned TypeScript comparator, eight copied-mutant semantic red controls, command/testserver binaries and registered conformance. Registered upstream versions remain `ai@7.0.107`, `@ai-sdk/gateway@4.0.87`, `@ai-sdk/react@4.0.110`, `@ai-sdk/provider@4.0.17` (all pins in `test/conformance/upstream.yaml`). No runtime behavior or upstream baseline was changed.
- `mise run verify-merged-pins`, `mise run verify-ai-gateway-boundary`, `mise run verify-sdk-gateway-isolation`, `mise run test-ai-gateway-source` — pass. The root workspace remains Gateway-free; candidate root/client compile and test with Gateway source physically absent.
- `MODULE=ai-gateway mise run verify-published-module` — pass independently using a fresh public-proxy module cache, readonly manifests and `GOWORK=off`. `mise run build-ai-gateway-image`, a local native container readiness smoke with network disabled and `docker buildx build --platform linux/amd64,linux/arm64 --output type=oci` — pass. These are local worktree checks of unchanged Gateway module/image inputs, not hosted proof at the eventual merged SHA. The on-demand all-module standalone task remains available but is not part of source PR CI.

## Rollout boundary

Read-only inspection of active canonical `main` ruleset `19928998` on 2026-09-25 found required check names `ci`, `docs-lint`, `module-resolution`, `parity-baseline`, `integration-test`, `conformance-test` and `Validate Changes`. This change retains those job identities and makes no settings edit. Reconfirm the live ruleset before rollout and obtain maintainer approval before any required-check change; coordinate changes without bypassing checks.

The local structural test does not prove hosted event evaluation or publication at the final revision. On the first eligible canonical main/Gateway-tag push, inspect the same-SHA standalone step before image validation, successful source jobs, publisher dependency results and main deployment ordering. Skipped, cancelled or failed image validation must not publish or deploy; stop and revert/fix if that guard breaks. Source PR success is not SDK release authorization: #21 must incorporate #245 before release automation is enabled; manual module publication still needs selected-module standalone validation.
