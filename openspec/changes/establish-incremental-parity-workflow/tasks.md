## 1. Deterministic target tooling

- [x] 1.1 Add failing tests for frozen selection/application, exact evidence, stale/mixed baselines, immutable output, no-mutation failures and resume.
- [x] 1.2 Implement explicit select/apply CLI and mise entry points, retaining mature coherent selection and all four consumers.
- [x] 1.3 Clear verification date only on new target application and reject missing/invalid dates in baseline validation; add regressions.

## 2. Repository workflow

- [x] 2.1 Write the operational runbook with source-backed discovery, approval, publication-aware merge planning, pause/resume and separate completion states.
- [x] 2.2 Align AGENTS, CONTRIBUTING, conformance README and upgrade/review skills; remove conflicting latest-at-every-run instructions.
- [x] 2.3 Validate a fixture-driven workflow walkthrough without the existing upgrade plan, and document remaining human assessment boundaries.

## 3. Local alignment and verification

- [x] 3.1 Back up and update relevant local automation configuration/prompts to repository-owned read-only discovery; validate and dry-run without launch.
- [x] 3.2 Run tooling/type/metadata/doc/OpenSpec checks and inspect all changes for scope, provenance and unchanged canonical pins/fixtures.
- [x] 3.3 Record concise results and local deployment prerequisites; leave spec sync/archive and commit/push for explicit approval.
