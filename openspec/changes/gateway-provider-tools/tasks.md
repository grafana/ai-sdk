## 1. Lock the WP13 evidence and scope

- [x] 1.1 Recheck merged WP11/WP12 code and `test/conformance/upstream.yaml`; compare all touched tool producers, prompt converters and tests at the registered commit/package versions, not the local upstream HEAD.
- [x] 1.2 Inventory supported native tool/result cases and metadata producer/replay pairs, including pinned Anthropic MCP call/result producer and prompt consumer. Define the explicit public metadata fields and validation: allow `anthropic.type='mcp-tool-use'` and a bounded `serverName` only for a caller-configured server in this request; never expose URL/token. Document non-MCP allowlist, privacy rationale and compound-flow limits before enabling mapping.
- [x] 1.3 Add pinned-client request/consumption cases for provider definitions, aliases, empty/nested args, request-level `providerOptions.anthropic.mcpServers` (token, tool configuration, empty/false), MCP tool metadata, assistant continuation and true/false/absent markers; add raw forbidden-member cases separately. Confirm current Gateway/client failures without modifying provider input provenance.

## 2. Apache provider domain and direct validation

- [x] 2.1 Add tests for equivalent marker normalization and ProviderExecuted true, plus distinct absent/false/true StreamPart.Dynamic input-start values; distinguish non-null output results from nullable prompt JSON result arms.
- [x] 2.2 Change provider-domain Preliminary and GenerateContentPart.Dynamic fields to bool, retain StreamPart.Dynamic *bool, and adapt consumers together. Correct input-start text-stream inference to let explicit false/true win and infer only on absence; separately project UI dynamic from known tool definitions or unknown tool text-stream values, matching pinned upstream. Preserve function Strict and unrelated core/UI APIs.
- [x] 2.3 Add focused tool-variant validation and integrate it before native I/O in existing Anthropic, OpenAI, Bedrock and OpenAI-compatible direct entry points as applicable. Normalize nil Go provider Args to empty-object semantics while rejecting malformed non-nil values and populated inactive fields; test zero transport calls for invalid tools without changing strict HTTP missing/null args validation.
- [x] 2.4 Preserve native unsupported-ID behavior and provider-defined client execution; run focused root/provider tests and verify valid function options and current provider factories remain usable.

## 3. Native conversion and reusable observability

- [x] 3.1 Reuse or import provenance-valid pinned Anthropic/OpenAI fixtures for affected provider-tool definitions and continuation, observing failing request/output expectations before any conversion fix. Use focused synthetic tests where authentic fixtures cannot express a case.
- [x] 3.2 Verify and fix only demonstrated native conversion gaps for IDs, aliases, object args, tool selection, Anthropic `mcp_servers`/MCP tool call and result replay, provider-owned versus client-owned calls, basic assistant results and supported tool metadata. Correct native Anthropic MCP token/enabled omission versus explicit empty/false with request-byte tests; publish a new immutable prerequisite and repin the Gateway. Assert native request snapshots, not only provider-domain equality.
- [x] 3.3 Adapt Agent Observability mapping/tests for normalized markers and preview replacement; prove provider-owned calls and final results remain correctly represented without counting preliminary events as separate completed tools.
- [x] 3.4 Add deterministic `test/integration/` scenarios and Vitest assertions using `parseJsonEventStream` and `uiMessageChunkSchema` for provider-executed behavior and input-start dynamic absent/false/true against known dynamic, known ordinary and unknown tools. Compare separately actual pinned/Go text-stream parts and UI chunks, not normalized DTOs; record the focused inference/UI correction.
- [x] 3.5 Add native unary tests proving context deadline permits the unmodified large default token budget, explicit request timeout retains precedence, expired deadline avoids I/O, and no-deadline behavior stays bounded. Implement the Anthropic-only timeout adaptation, run isolated tests, publish its immutable prerequisite and repin the Gateway.

## 4. Independent Apache Go Gateway client

- [x] 4.1 Extend `providers/grafana` unary decoding to provider calls/results and supported tool metadata, with non-null result checks, marker normalization, bounded reads and unchanged client-owned request/response/warnings replacement.
- [x] 4.2 Extend incremental SSE decoding for markers and metadata, preserving input-start dynamic presence and accepting result-only continuation without a repeated call. Do not import server DTOs or duplicate server lifecycle state. Test malformed markers, oversized events, clean EOF, final-before-EOF and canceled blocked delivery.
- [x] 4.3 Verify Go client request projection emits `args: {}` for nil provider Args, preserves explicit object args, `providerOptions.anthropic.mcpServers` (including sensitive optional token) and assistant MCP continuation/options; keep forbidden definition fields rejected before token acquisition or HTTP. Update affected structural contract witnesses only with reviewed evidence.
- [x] 4.4 Run focused client tests and native prerequisite checks, publish ordered immutable Apache prerequisites and update affected module requirements. Verify proxy resolution and released-root compatibility rather than relying only on `go.work`.

## 5. AGPL strict request and unary capability

- [x] 5.1 Pin Gateway dependencies to the tested immutable Apache versions; verify `GOWORK=off` compilation without committed replacements before dependent implementation.
- [x] 5.2 Add provider-definition mapping beside the function arm; separately support only registered request-level Anthropic `mcpServers` with validation and bounded destination/field limits. Gate forwarding using the configured direct route type in service composition before physical invocation, not `ResolvedModel.Model.Provider()` after logical wrapping. Replay pinned captures; test forbidden definition fields, invalid server shapes/destinations, unsupported root options, fallback and backend mismatch with no secret leak.
- [x] 5.3 Map provider-executed assistant calls and assistant-side basic results, including tool-part continuation options and host-namespace rejection. Derive request-local eligible unresolved provider-call identities from bounded history, excluding completed and client-owned calls without retaining copied payloads. Preserve role rules and deferred-family rejection.
- [x] 5.4 Implement reviewed tool-metadata projection/round-trip from task 1.2, including bounded `anthropic.type/serverName` on MCP calls/results matched to request-level server definitions; reject unconfigured/malformed required shapes, omit private/unknown fields and bound metadata before copying.
- [x] 5.5 Extend private unary DTOs and test-only success schema for calls/results and registered markers. Correlate results against current output and eligible history; test result-only success/error continuation, invalid matches, value variants, ordered content, precommit failure, hostile metadata and exact/over-limit bytes.

## 6. AGPL streaming lifecycle

- [x] 6.1 Extend private stream DTOs and test-only event schema for registered marker placement and metadata; preserve input-start dynamic absent/false/true and required empty input deltas, and omit the unregistered result-level providerExecuted field.
- [x] 6.2 Extend per-ID state with eligible unresolved provider calls from request history, repeated preliminary results and finalization. Allow complete provider calls to finish without a result and accept later result-only success/error streams. Test interleaving, unknown IDs, mismatched names, completed/client-owned historical IDs, duplicates, result-after-final and finish-with-pending-preview; bound history state separately from consumed provider parts.
- [x] 6.3 Add hostile lifecycle tests for preview floods, result/metadata frame bounds, non-terminal provider errors, writer failure, cancellation during input/results and bounded drain. Verify authoritative finish and immediate clean EOF remain unchanged.

## 7. Both-client service acceptance

- [x] 7.1 Extend the pinned Vercel/Go differential suite for semantic requests, equivalent disabled marker normalization, distinct input-start dynamic presence, true preservation, result/error values, dynamic names, previews/final and metadata. Observe zero local executions for hosted calls and retain a provider-defined client-executed control.
- [x] 7.2 Exercise unary and streaming continuation through both actual clients and the real handler: call → finish without result → next request with unresolved call history → result-only success/error response. Assert no repeated call is required, invalid historical matches fail, and full history/metadata-dependent native replay works without Gateway session state or local execution. Distinguish explicit client calls from any automatic core loop behavior actually tested.
- [x] 7.3 Extend authenticated real-command tests with deterministic Anthropic code execution and hosted MCP call/result, aliased names, caller-supplied server configuration (URL, optional token, toolConfiguration), and native continuation assertions. Prove only selected Anthropic transport sees credentials; public outputs and telemetry omit URL/token. Test unconfigured names, malformed destinations and safe rejection of deferred compound output separately.
- [x] 7.4 Verify one canonical logical generation per request, expected usage/finish/error accounting, and no tool/metadata/credential/private-backend markers in metadata-only AO, logs or metrics. Prove provider definitions, history and nonempty MCP configuration alone cause zero candidate calls on fallback routes in both HTTP modes and clients.

## 8. Verification and support documentation

- [x] 8.1 Run focused root, affected native-provider, Grafana client and Agent Observability module tests plus relevant race tests; run `mise run build`, `mise run vet` and `mise run lint` for the changed surfaces.
- [x] 8.2 Run `mise run parity-check` and `mise run test-integration`; inspect changed request/UI expectations, preserve authentic provider inputs and classify any remaining differences or proof gaps.
- [x] 8.3 Run `mise run test-providerwire-v4`, `mise run test-ai-gateway-command` and `mise run test-ai-gateway` against committed Gateway pins with `GOWORK=off`; run `mise run verify-module-resolution` and `mise run verify-ai-gateway-boundary`.
- [x] 8.4 Update `test/conformance/PARITY.md` only for proven stable coverage/support boundaries; document boolean-field migration/input-start exception, history-aware deferred result correlation, exact provider-tool rules, Anthropic request-level MCP configuration and public name-only metadata (not URL/token), remaining WP21 root options, deferred compound flows and effect-disabled fallback in SDK/Gateway docs. State that Vercel-hosted MCP policy is unknown.
- [x] 8.5 If enabling WP13 in a deployed image, review caller-supplied MCP destination/token and Anthropic remote egress policy, extend rollout smoke and revalidate notices, source revision and corresponding-source offer; otherwise explicitly record deployment activation as outside verified implementation evidence.
- [x] 8.6 Review the finished diff against the same pinned upstream sources and issue #107 acceptance criteria; confirm no later-package expansion, fabricated fixture evidence, local dependency replacements or Apache-to-Gateway imports remain.
