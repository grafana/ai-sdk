## 1. Establish provider-boundary regressions

- [ ] 1.1 Add deterministic HTTP/fake transport tests in `providers/anthropic` for both direct and Vertex, generate and stream, asserting transmitted per-call Agent marker, case-insensitive ordinary-header override, configured+call `anthropic-beta` token merge/deduplication, SDK-generated beta tokens and authentication; prove the current omission before changing implementation.
- [ ] 1.2 Add tests in `providers/anthropic` and `providers/openai` asserting byte equality between successfully returned unary/stream request JSON and the HTTP endpoint's actually received body, plus actual returned response headers and unary raw response JSON; cover Vertex model/version rewrites, shared-reader `GetBody` alias, concurrency, configured clients, and SDK retries; prove the current returned-metadata omission without consuming the outbound body.
- [ ] 1.3 Add provider stream tests for omitted/false/true `IncludeRawChunks`, buffered first events, valid/unknown events, observable malformed OpenAI frames (nil raw then error, then continued events), parsed OpenAI post-acceptance error envelopes (JSON raw then error), and initial errors (returned error/no stream). Check Anthropic SDK-hidden malformed/error/ping behavior against pinned upstream without declaring full parity; cover raw/normalized ordering, preflight, and >64 pending events with stalled-consumer cancellation/body closure. Use focused synthetic fake transports, never invented provider-recorded fixtures.
- [ ] 1.4 Verify installed Go SDK middleware order, retry behavior and response-capture options against SDK source and deterministic HTTP tests; demonstrate that Anthropic Vertex `GetBody` aliases `Body`, and verify byte capture from forwarded request reads rather than reading the alias. Classify every hidden Anthropic frame against the registered TypeScript source/tests; if parity needs an unapproved decoder/transport redesign, escalate for explicit disposition instead of asserting coverage or permanent deviation.

## 2. Implement SDK adapter transport context

- [ ] 2.1 Pass ordinary Anthropic `CallOptions.Headers` via call-local `option.WithHeader` after configured request options in `DoGenerate` and `DoStream`; merge `anthropic-beta` configured/SDK/call tokens case-insensitively through call-local middleware after SDK header setup, preserving direct/Vertex SDK auth and beta options.
- [ ] 2.2 Capture each successful attempt's transmitted JSON by teeing outgoing `req.Body` with a forwarding/closing wrapper inside a call-local SDK middleware, never pre-reading an aliasing `GetBody`; match byte counts and retain only the final successful attempt's owned copy (omit optional body when unverifiable). Capture actual successful response headers with SDK call-level response options; populate existing generate/stream `Request` and `Response` fields, retain unary OpenAI `RawJSON()` and populate Anthropic unary raw response JSON without reading the streaming response body.
- [ ] 2.3 Carry owned raw JSON or a nil syntax-failure marker through Anthropic/OpenAI decoded stream items, including OpenAI's buffered preflight; emit opt-in `PartRaw` before corresponding normalized/error parts for exposed events (including OpenAI's post-acceptance parsed error envelope). Preserve initial-error preflight, SDK-hidden Anthropic frames, decoder ownership and absence of extra finish parts; do not claim full pinned parity for SDK-hidden frames without a reviewed disposition.
- [ ] 2.4 Thread `ctx` and a shared context-aware send function through Anthropic `consumeStream`, `streamAdapter.handleEvent` and all converter helper emissions, including raw and errors. Propagate cancellation without a spurious error, close the stream body from its single owner even when the channel is full and the consumer stops, and verify the existing OpenAI pump's cancellation remains intact.

## 3. Expose core metadata without crossing Gateway privacy boundary

- [ ] 3.1 Add core tests for `StreamText` step, callback and final response/request metadata across single and multiple steps, plus `GenerateText`/Agent stream-based result access; verify a metadata part without headers retains stream-result headers and a non-nil event header value overrides them.
- [ ] 3.2 Initialize core step response headers from `StreamResult.Response`, retain explicit event-level precedence, and keep request metadata step-local; do not alter public SSE serialization just to expose transport fields.
- [ ] 3.3 Extend Gateway privacy regression tests using secret-valued provider request/response/raw data in unary and streaming modes; assert existing allowlisted success/error normalization and unsupported-raw behavior remain unchanged.

## 4. Validate parity and independent delivery

- [ ] 4.1 Run focused root and separate Anthropic/OpenAI module tests (including `-race` for concurrent capture), initial error/preflight/auth regression tests, Gateway tests, and public-module resolution (`mise run verify-module-resolution`); adjust coverage only for concrete failing assertions.
- [ ] 4.2 Check frontend wire impact: if provider raw events change UI chunks, add a deterministic testserver/Vitest scenario parsed with `parseJsonEventStream` and `uiMessageChunkSchema`, and run `mise run test-integration`; otherwise document why HTTP/core-only changes leave frontend chunks unchanged.
- [ ] 4.3 Run `mise run parity-check` on committed parity behavior, review existing expected request/UI snapshots for changes, and update only genuinely changed expectations from authentic existing inputs. Do not invent or modify `recorded/input*.chunks.txt`; update `test/conformance/PARITY.md` only if stable evidence or coverage status changes.
