## 1. Establish provider-boundary regressions

- [x] 1.1 Add deterministic HTTP/fake transport tests in `providers/anthropic` for both direct and Vertex, generate and stream, asserting transmitted per-call Agent marker, case-insensitive ordinary-header override, configured+call `anthropic-beta` token merge/deduplication, SDK-generated beta tokens and authentication; prove the current omission before changing implementation.
- [x] 1.2 Add tests in `providers/anthropic` and `providers/openai` asserting byte equality between successfully returned unary/stream request JSON and the HTTP endpoint's actually received body, plus actual returned response headers and unary raw response JSON; cover Vertex model/version rewrites, shared-reader `GetBody` alias, concurrency, configured clients, and SDK retries; prove the current returned-metadata omission without consuming the outbound body.
- [x] 1.3 Add provider stream tests for omitted/false/true `IncludeRawChunks`, buffered first events, valid/unknown events, observable malformed OpenAI frames (nil raw then error, then continued events), parsed OpenAI post-acceptance error envelopes (JSON raw then error), and initial errors (returned error/no stream). Check intercepted Anthropic malformed/error/ping frames against pinned upstream, including LF/CRLF, multiline data, split reads, unknown events, raw/normalized ordering, preflight, and >64 pending events with stalled-consumer cancellation/body closure. Use focused synthetic fake transports, never invented provider-recorded fixtures.
- [x] 1.4 Verify installed Go SDK middleware order, retry behavior and response-capture options against SDK source and deterministic HTTP tests; demonstrate that Anthropic Vertex `GetBody` aliases `Body`, and verify byte capture from forwarded request reads rather than reading the alias. Prove a call-local forwarding SSE response-body wrapper can expose SDK-filtered frames in order while retaining SDK decoder error/preflight behavior, body ownership, cancellation and SDK authentication; classify any remaining gap against the pinned source and escalate for explicit disposition.

## 2. Implement SDK adapter transport context

- [x] 2.1 Pass ordinary Anthropic `CallOptions.Headers` via call-local `option.WithHeader` after configured request options in `DoGenerate` and `DoStream`; merge `anthropic-beta` configured/SDK/call tokens case-insensitively through call-local middleware after SDK header setup, preserving direct/Vertex SDK auth and beta options.
- [x] 2.2 Capture each successful attempt's SDK-submitted JSON by teeing outgoing `req.Body` with a forwarding/closing wrapper inside a call-local SDK middleware, never pre-reading an aliasing `GetBody`; match byte counts and retain only the final successful attempt's owned copy (omit optional body when SDK-boundary capture is incomplete). A downstream caller-supplied body-rewriting transport is outside the exact-wire guarantee. Capture actual successful response headers with SDK call-level response options; populate existing generate/stream `Request` and `Response` fields, retain unary OpenAI `RawJSON()` and populate Anthropic unary raw response JSON without reading the streaming response body.
- [x] 2.3 Carry owned raw JSON or a nil syntax-failure marker through OpenAI decoded items and Anthropic's per-call forwarding response-body wrapper; emit opt-in `PartRaw` in exact SSE frame order before corresponding normalized/error parts, including SDK-filtered Anthropic ping/malformed/error and OpenAI's post-acceptance parsed error envelope. Preserve initial-error preflight, one SDK-owned response body, cancellation and absence of extra finish parts; retain SDK-filtered malformed ping/unknown-event parser errors as the explicitly documented limited-support boundary rather than claiming full pinned error parity.
- [x] 2.4 Route Anthropic converter and raw parts through a bounded internal channel and a context-aware forwarder that drains on cancellation, following the existing OpenAI pattern. Check context before each SDK `stream.Next()` and error emission, close the stream body from its single owner even when the public channel fills and the consumer stops, and verify the existing OpenAI pump's cancellation remains intact.

## 3. Expose core metadata without crossing Gateway privacy boundary

- [x] 3.1 Add core tests for `StreamText` step, callback and final response/request metadata across single and multiple steps, plus `GenerateText`/Agent stream-based result access; verify a metadata part without headers retains stream-result headers and a non-nil event header value overrides them.
- [x] 3.2 Initialize core step response headers from `StreamResult.Response`, retain explicit event-level precedence, and keep request metadata step-local; do not alter public SSE serialization just to expose transport fields.
- [x] 3.3 Extend Gateway privacy regression tests using secret-valued provider request/response/raw data in unary and streaming modes; assert existing allowlisted success/error normalization and unsupported-raw behavior remain unchanged.

## 4. Validate parity and independent delivery

- [x] 4.1 Run focused root and separate Anthropic/OpenAI module tests (including `-race` for concurrent capture), initial error/preflight/auth regression tests, Gateway tests, and public-module resolution (`mise run verify-module-resolution`); adjust coverage only for concrete failing assertions.
- [x] 4.2 Check frontend wire impact: if provider raw events change UI chunks, add a deterministic testserver/Vitest scenario parsed with `parseJsonEventStream` and `uiMessageChunkSchema`, and run `mise run test-integration`; otherwise document why HTTP/core-only changes leave frontend chunks unchanged.
- [x] 4.3 Run `mise run parity-check` on committed parity behavior, review existing expected request/UI snapshots for changes, and update only genuinely changed expectations from authentic existing inputs. Do not invent or modify `recorded/input*.chunks.txt`; update `test/conformance/PARITY.md` only if stable evidence or coverage status changes.
