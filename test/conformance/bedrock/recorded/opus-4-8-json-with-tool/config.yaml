model: us.anthropic.claude-opus-4-8
prompt: "Return the current status as JSON."
responseFormat:
  type: json
  schema:
    properties:
      status:
        type: string
    required: [status]
    type: object
tools:
  weather:
    description: Get the current weather for a city.
    inputSchema:
      type: object
      properties:
        city:
          type: string
