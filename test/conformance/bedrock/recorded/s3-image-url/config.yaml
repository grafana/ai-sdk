model: us.amazon.nova-lite-v1:0
prompt: Describe an image stored in S3.
messages:
  - role: user
    content:
      - type: text
        text: Describe the image.
      - type: file
        url: s3://${BEDROCK_S3_BUCKET}/fixture/image.png
        mediaType: image/png
