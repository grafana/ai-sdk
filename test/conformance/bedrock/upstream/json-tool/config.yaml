# Multi-step JSON tool flow. The fixture has 3 sequential model responses
# captured during a multi-step tool calling conversation that ends with a
# JSON-formatted answer.
model: anthropic.claude-3-haiku-20240307-v1:0
stopWhenStepCount: 3
responseFormat:
  type: json
  schema:
    type: object
    properties:
      answer:
        type: string
tools:
  weather:
    description: Get the current weather for a city.
    inputSchema:
      type: object
      properties:
        city:
          type: string
      required: [city]
    mockResults:
      - { temperature: 22, conditions: "sunny" }
