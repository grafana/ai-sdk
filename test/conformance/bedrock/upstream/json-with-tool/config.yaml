# JSON response format with a tool definition that doesn't fire on this
# step. Verifies the synthetic json tool injection works alongside a
# user-defined tool.
model: anthropic.claude-3-haiku-20240307-v1:0
responseFormat:
  type: json
  schema:
    type: object
    properties:
      answer:
        type: string
tools:
  weather:
    description: Get the current weather for a city.
    inputSchema:
      type: object
      properties:
        city:
          type: string
