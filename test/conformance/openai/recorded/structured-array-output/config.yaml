model: gpt-4o
prompt: "Generate a JSON array of named items."
responseFormat:
  type: json
  outputMode: array
  name: items
  schema:
    type: object
    properties:
      name:
        type: string
    required:
      - name
    additionalProperties: false
